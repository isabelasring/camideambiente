// Middlewares personalizados
// Aquí irán los middlewares que necesites (autenticación, validación, etc.)

// Ejemplo de middleware:
/*
const exampleMiddleware = (req, res, next) => {
  // Lógica del middleware
  next();
};
*/

module.exports = {};
