// Utilidades y funciones auxiliares
// Aquí irán funciones reutilizables

// Ejemplo de función utilitaria:
/*
const formatDate = (date) => {
  return new Date(date).toLocaleDateString();
};
*/

module.exports = {};
